[user]
    email = jjcrosskey@quantmt.com
    name = JJ Crosskey
[alias]
    co = checkout
    ci = commit
    cm = commit -m 
    st = status
    br = branch
    rv = remote -v
    po = push origin 
    hist = log --pretty=format:\"%h %ad | %s%d [%an]\" --graph --date=short
    type = cat-file -t
    dump = cat-file -p
[push]
    default = simple
[core]
    excludesfile = /home/jjcrosskey/.gitignore_global
